# Google Wallet Configuration
# Get these values from your Google Cloud Console:
# 1. Go to https://console.cloud.google.com
# 2. Enable the Google Wallet API
# 3. Create a service account
# 4. Download the JSON key file

GOOGLE_WALLET_ISSUER_ID=your_issuer_id_here
GOOGLE_WALLET_SERVICE_ACCOUNT=your-service-account@project.iam.gserviceaccount.com

# The private key should be stored in a separate file (e.g., service-account-key.pem)
# Extract it from the JSON key file you downloaded
